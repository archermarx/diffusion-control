[project]
name = "diffusion-control"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hall-diffusion",
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "pandas>=3.0.3",
    "scipy>=1.17.1",
    "torch>=2.12.0",
    "watchdog>=6.0.0",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.uv.sources]
hall-diffusion = { path = "../hall_diffusion", editable = true }
